project: rl-practice
run_name: independent-ql-lineworld
env_id: line_world
seed: 42
env_kwargs:
  n_agents: 2
  grid_length: 7
  max_steps: 80
  goal_positions: null
  step_penalty: -0.02
  goal_reward: 1.0
  shared_goal_bonus: 0.5
  collision_penalty: -0.1
total_episodes: 4000
max_steps_per_episode: 120
gamma: 0.95
alpha: 0.1
epsilon_start: 1.0
epsilon_end: 0.05
epsilon_decay: 0.0008
log_interval: 50
checkpoint_interval: 500
checkpoint_dir: Independent-QL/checkpoints
save_best: true
log_level: INFO
log_to_file: false
log_to_console: true
inference_model_path: Independent-QL/checkpoints/best.pt
episodes: 5
device: auto
